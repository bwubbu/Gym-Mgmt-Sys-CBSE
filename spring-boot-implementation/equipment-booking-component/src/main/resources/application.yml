server:
  port: 8083

spring:
  application:
    name: equipment-booking-component

# Service URLs
services:
  member:
    url: http://localhost:8081

# Logging configuration
logging:
  level:
    com.gymmanagement.equipmentbooking: INFO
    org.springframework.web: INFO
